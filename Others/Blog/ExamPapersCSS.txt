nav{
    position: fixed; /* Keeps the nav in the same place even when you scroll */
    height: 75px; /* Fixes the height of the nav to 75 pixels */
    width: 85%;
    background-color: #4d75aa; /* Changes the background color to light blue with high priority */
    display: flex; /* in a horzontal line */
    justify-content: space-between; /* Distributes space between items, placing the first on the left and last on the right */
    padding: 0.5vw 6.81vw; /* Adds spacing inside the nav: smaller padding top and bottom, larger on left and right */
    align-items: center; /* Centers items vertically in the nav */
    box-shadow: 2px 2px 16px rgba(248, 89, 89, 0.15); /* Adds a shadow for depth with a light opacity */
    z-index: 1000; /* Nothing will be overlapped */
    top:0px;
}
   
nav img {
    width: 130px; /* Force this width to be applied */
    height: auto; /* Maintain aspect ratio */
}

nav .items ul {
    display: flex;
    gap: 1rem;
   
}

nav .items ul li {
    list-style: none;
    
}

nav .items ul li a {
    text-decoration: none;
    color: rgb(1, 1, 14);
    font-size: 20px; /* Slightly reduced font size */
    font-weight: 600;
    transition: 0.3s;
}

nav .items ul li a.active, /* Placing the yellow color */

nav .items ul li a:hover {
    color: yellow;
}


                                                    /* Exam Papers Page */

/* O/l English Past Paper */

.past-papers-section {
    margin-top: 60px;
    padding: 20px;
    background-color: #f8f8f8;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

#section-title1 {
    text-align: center;
    font-size: 2em;
    scroll-margin-top: 100px; /* To take the exact place of the place. This issue often happens when a fixed header or navigation bar is present on the page, causing the anchor link to position below the intended section.

    Here are a few ways to solve this:
    
    1. Use CSS Scroll Margin
    If you’re using modern browsers, you can add a scroll margin to the target section's heading:*/
    color: #333;
}

.card-container {
    display: flex;
    flex-wrap: wrap; /* Allows items to wrap onto a new line */
    justify-content: center;
    gap: 30px;
    
}

.card {
    background: linear-gradient(135deg, #6bd3c0, #93bd4f);
    color: #fff;
    padding: 15px;
    border-radius: 10px;
    min-width: 250px;
    max-width: 300px;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
}

.card h3 {
    margin: 0 0 10px;
}

.card p {
    margin: 0 0 15px;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
}

.download-btn {
    text-decoration: none;
    color: #fff;
    background: #007bff;
    padding: 10px 15px;
    border-radius: 5px;
    display: inline-block; /* in between */
    
}

.download-btn:hover {
    background: #0056b3;
}

/* G.English Past Papers */


.al-papers-section {
    padding: 25px;
    margin: 20px auto;
    max-width: 1200px;
    background: #ffffff; /* White background to separate content visually */
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
}

/* Title styling for the section */
#section-title2 {
    text-align: center;
    font-size: 2.2em;
    color: #333;
    scroll-margin-top: 100px;
    
}

/* Grid container to display cards in a grid layout */
.grid-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); /* Responsive grid columns */
    gap: 20px; /* Space between cards */
}

/* Styling for individual grid items (cards) */
.grid-item {
    background: #ffcccb; /* Light coral color for better visibility */
    padding: 20px;
    border-radius: 8px;
    transition: transform 0.3s, box-shadow 0.3s; /* Smooth hover animations */
}

/* Heading for each card */
.grid-item h3 {
    margin-top: 0;
}

/* Button inside the card */
.view-btn {
    text-decoration: none;
    color: #fff;
    background: #ff6347; /* Tomato color for the button */
    padding: 8px 15px;
    border-radius: 4px;
    display: inline-block;
    margin-top: 10px;
    
}

/* Hover effect for the card */
.grid-item:hover {
    transform: scale(1.05); /* Slight scale-up on hover Zoom in and out*/
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15); /* Enhanced shadow */
}

/* Hover effect for the button */
.view-btn:hover {
    background: #e5533d; /* Slightly darker color for hover */
}

/* Model Papers for both */

/* Styling for the model papers section */
.model-papers-section {
    padding: 20px;
    text-align: center; /* Centering the content */
}

/* Styling for the section title */
#section-title3 {
    margin-bottom: 15px;
    font-size: 2em;
    color: #333;
    scroll-margin-top: 100px;
}

/* Styling for the horizontal list of years */
.year-list {
    display: flex;
    justify-content: center; /* Centers the years horizontally */
    gap: 15px; /* Space between the years */
}

/* Styling for each year link */
.year-link {
    display: inline-block;
    padding: 10px 20px;
    background: #4caf50; /* Green background color */
    color: white;
    text-decoration: none;
    border-radius: 5px;
    
}

/* Hover effect for each year link */
.year-link:hover {
    background: #388e3c; /* Darker green on hover */
    transform: translateY(-3px); /* Slight lift effect */
}


/* Footer */

footer {
    padding: 6vw;
    background-color: #002753;
    justify-content: space-between;
    align-items: flex-start;
    flex-wrap: wrap;
    display: flex;
    flex-direction: row;
}

footer .footer-col {
    padding-bottom: 40px;
    
}

footer h3 {
    color: white;
    font-weight: 600;
    padding-bottom: 20px;
}

footer li, .Anchor{
    list-style: none;
    color: gray;
    padding: 10px 0;
    font-size: 15px;
    cursor: pointer;
    transition: 00.3s ease;
    text-decoration: none;
}


footer li:hover , .Anchor:hover {
    color: white;
}

.Copyright {
    width: 100%;
    text-align: center;
    margin-top: auto;
    margin-bottom: 10px; /* Adjust this value to control how far down it goes */
    color: white;
}


.prolinks i {
    color: white;
    padding: 0 10px;
    cursor: pointer;
}

footer .Subscribe {
    margin-top: 20px;
}

footer input {
    width: 220px;
    padding: 15px 12px;
    background: rgb(91, 91, 201);
    border: none;
    outline: none;
    color: white;
}

footer .subscribe a{
    text-decoration: none;
    font-size: 0.9rem;
    padding: 13px 40px;
    background-color:white;
    font-weight: 600;
    border-radius: 5px;
}


footer .subscribe a.yellow{
    color: rgb(0, 0, 0);
    background: rgb(177, 177, 40);
    transition: 0.3s ease;
}

footer .subscribe a.yellow:hover{
    color: rgb(255, 94, 0);
    background: white;
}

.footer-col p{
    color:gray;
}


.subscribe input::placeholder {
    color: rgb(229, 229, 243);
    font-weight: 500;
    font-size: 0.9rem;
}